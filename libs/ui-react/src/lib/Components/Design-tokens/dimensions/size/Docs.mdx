import { Markdown, Meta, Canvas } from '@storybook/addon-docs/blocks';
import sizeTaxonomy from '../../images/size-taxonomy.png';
import sizeTokenExample from '../../images/token-size-example.png';
import * as Size from './size.stories';

<Meta title="Foundations/Size/Docs" of={Size} />

# Size

Width and height define the fixed dimensions of elements, controlling their span and contributing to layout consistency.

## ⚡ Structure

All size tokens follow the structure: `size-{size}` where the size value represents **pixels**.

<img src={sizeTaxonomy} alt="Size token taxonomy structure" />

Example: `size-44`

<Markdown>
  {`
| Element | Definition | Example |
| ------- | ---------- | ------- |
| size | Defines fixed dimensions of element | size |
| Size | Defines the value in pixels | 0, 1, 2, 4, 6, 8, 10, 12, 14.. |
`}
</Markdown>

## 📐 Size Scale

Size tokens provide a consistent set of dimensions that can be used for both width and height properties. These tokens ensure that elements maintain proportional and harmonious relationships across the interface.

### Usage

Size tokens can be applied in three ways:

- As a square dimension using the `size-*` class
- As a width using the `w-*` class
- As a height using the `h-*` class

<Markdown>
{`
\`\`\`jsx
// Square element (both width and height)
<div className="size-32" />

// Width only

<div className="w-32" />

// Height only

<div className="h-32" />
\`\`\`
`}
</Markdown>

### Available Values

<Canvas></Canvas>

**👨‍💻 For developers:** this table below shows the Tailwind class used to define
the size of an element using the `size-*` token.

<div className="my-24 overflow-hidden rounded-lg">
  <table className="w-full">
    <thead>
      <tr className="border-b border-muted bg-muted">
        <th className="p-12 text-left text-on-accent body-3">
          Tailwind class (utilities)
        </th>
        <th className="p-12 text-left text-on-accent body-3">styles</th>
      </tr>
    </thead>
    <tbody className="bg-canvas">
      <tr className="border-b border-muted">
        <td className="text-accent p-12 body-4">size-1</td>
        <td className="p-12 text-muted body-4">
          width: var(--size-1); height: var(--size-1); /* 1px */
        </td>
      </tr>
      <tr className="border-b border-muted">
        <td className="text-accent p-12 body-4">size-4</td>
        <td className="p-12 text-muted body-4">
          width: var(--size-4); height: var(--size-4); /* 4px */
        </td>
      </tr>
      <tr className="border-b border-muted">
        <td className="text-accent p-12 body-4">size-128</td>
        <td className="p-12 text-muted body-4">
          width: var(--size-128); height: var(--size-128); /* 128px */
        </td>
      </tr>
      <tr>
        <td className="text-accent p-12 body-4">size-256</td>
        <td className="p-12 text-muted body-4">
          width: var(--size-256); height: var(--size-256); /* 256px */
        </td>
      </tr>
    </tbody>
  </table>
</div>

### Display Size Tokens

<img src={sizeTokenExample} alt="Size Token Examples" />
