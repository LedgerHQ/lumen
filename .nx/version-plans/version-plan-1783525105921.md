---
'@ledgerhq/lumen-ui-react-visualization': patch
---

refactor(charts): restructure charts internals

- Consolidate all chart constants into a single source-of-truth config object
- Deduplicate shared types across chart components, and decouple geometry computation from rendering (extracting `useReferenceLineGeometry` and `useScrubberGeometry` hooks).
- Introduces a `mergeDefaults` helper and tightens type safety. No public API changes.
