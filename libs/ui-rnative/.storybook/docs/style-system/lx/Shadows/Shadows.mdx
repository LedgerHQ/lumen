import { Meta } from '@storybook/addon-docs/blocks';
import { ShadowTable } from './ShadowTable';

<Meta title='Style System/Theme/Shadows' />

# 🌑 Shadows

Our shadow system provides elevation and depth to UI elements, creating visual hierarchy.

## Shadows

Shadows are accessed through the `shadowStyle` prop in the `lx` style object or by directly using `theme.shadows.*`.

<ShadowTable />
