---
'@ledgerhq/lumen-ui-rnative-visualization': patch
---

feat(DonutChart): add `useDonutSeries` and its pure `prepareDonutSeries` counterpart for sorting and grouping a raw series, and expose a display-ready `percentLabel` on the active segment alongside the exact `percent`
