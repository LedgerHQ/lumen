export const DEFAULT_DURATION_IN_SECONDS = 0.8;
export const DEFAULT_EASING = 'linear';

/** Duration in seconds of an individual point's opacity fade-in. */
export const POINT_FADE_DURATION_IN_SECONDS = 0.2;
/**
 * Offset (in seconds) applied to the point fade-in relative to the end of the
 * clip-path reveal. Negative so points start fading in slightly before the
 * line reveal completes.
 */
export const POINT_FADE_IN_AFTER_CLIP_IN_SECONDS = -0.1;
