import { Markdown, Meta, Canvas } from '@storybook/addon-docs/blocks';
import borderWidthTaxonomy from '../images/border-width-Taxonomy.png';
import * as BorderWidth from './borderWidth.stories';
import tokenExample from '../images/Token-border-width-example.png';

<Meta title="Foundations/Border/Width" of={BorderWidth} />

# Border Width

Border thickness is controlled by border width token, providing a consistent and scalable way to define stroke weight across a design language system.

## ⚡ Structure

All width tokens follow the structure: `border-width-state`

Example: `border-width-active`

<img src={borderWidthTaxonomy} alt="Border width token taxonomy structure" />

<Markdown>
  {`
| Element  | Definition | Example |
| -------- | ---------- | ------- |
| Property | define width of the border of element | border-width |
| State    | define the different state of the token | default, active, focus |
`}
</Markdown>

## 🔄 States

<Canvas></Canvas>
**👨‍💻 For developers:** this table below shows the Tailwind class used to define
the width of the border of an element using the `border-width-*` token.

<div className="my-24 overflow-hidden rounded-lg">
  <table className="w-full">
    <thead>
      <tr className="border-b border-muted bg-muted">
        <th className="p-12 text-left text-on-accent body-3">
          Tailwind class (utilities)
        </th>
        <th className="p-12 text-left text-on-accent body-3">styles</th>
      </tr>
    </thead>
    <tbody className="bg-canvas">
      <tr className="border-b border-muted">
        <td className="text-accent p-12 body-4">border</td>
        <td className="p-12 text-muted body-4">
          border-width: var(--border-width); /* 1px */
        </td>
      </tr>
      <tr className="border-b border-muted">
        <td className="text-accent p-12 body-4">border-2</td>
        <td className="p-12 text-muted body-4">
          border-width: var(--border-width-active); /* 2px */
        </td>
      </tr>
    </tbody>
  </table>
</div>

**🎨 For designers:** this table below shows the token used to define the width of the border of an element.

<Markdown>
  {`
| Token | Value | State | Usage |
| ----- | ----- | ----- | ----- |
| border-width-default | 1px | Default | Standard border width for elements in their normal state |
| border-width-active | 2px | Active | Enhanced border width for active or selected elements |
| border-width-focus | 2px | Focus | Prominent border width for focused elements |
`}
</Markdown>

## Example Implementation

<img src={tokenExample} alt="Token example diagram" />
