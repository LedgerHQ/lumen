import { Meta } from '@storybook/addon-docs/blocks';
import { BorderRadiusTable } from './BorderRadiusTable';

<Meta title='Style System/Theme/Border Radius' />

# 🔘 Border Radius

Our border radius system provides consistent corner rounding values for UI elements.

## Border Radius Values

Border radius values are accessed through `theme.borderRadius.*` and can be used for rounded corners on boxes, cards, buttons, and other UI elements.

<BorderRadiusTable />

