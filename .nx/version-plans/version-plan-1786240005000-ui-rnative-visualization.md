---
'@ledgerhq/lumen-ui-rnative-visualization': patch
---

refactor(Scrubber): move the default magnet radius into `chartConfig.scrubber.defaultMagnetRadius` and rename `applyMagnetisation` to `applyMagnetization`
