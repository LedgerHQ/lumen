---
'@ledgerhq/lumen-ui-rnative': patch
---

feat(OptionList): add OptionList composable selection list with data-driven grouping, sub-components, tests, stories, and docs.

**Deprecation notice:** OptionList replaces Select. Select will be deprecated in upcoming versions — new selection UIs should use OptionList compositions (e.g. inside BottomSheet).

### Migrating from Select to OptionList

#### 1. Rendering — renderItem replaces static children

Select uses component introspection (`SelectItem` / `SelectItemText` return `null`). OptionList gives you full rendering control via `renderItem`:

```diff
- <SelectContent>
-   <SelectItem value="btc">
-     <SelectItemText>Bitcoin</SelectItemText>
-   </SelectItem>
- </SelectContent>

+ const items: OptionListItemData[] = [
+   { value: 'apple', label: 'Apple', group: 'Fruits' },
+   { value: 'banana', label: 'Banana', group: 'Fruits' },
+   { value: 'carrot', label: 'Carrot', group: 'Vegetables' },
+ ];

+ <OptionListContent
+   renderItem={(item) => (
+     <OptionListItem value={item.value}>
+       <OptionListItemContent>
+         <OptionListItemText>{item.label}</OptionListItemText>
+         {item.description && (
+           <OptionListItemDescription>{item.description}</OptionListItemDescription>
+         )}
+       </OptionListItemContent>
+     </OptionListItem>
+   )}
+ />
```

#### 2. Bottom sheet — you own it

Select manages a global bottom sheet internally via `GlobalSelectProvider`. OptionList does not — you compose it inside your own `BottomSheet`:

```diff
- <Select value={value} onValueChange={setValue}>
-   <SelectTrigger label="Currency">
-     <SelectValue />
-   </SelectTrigger>
-   <SelectContent>
-     <SelectItem value="btc"><SelectItemText>Bitcoin</SelectItemText></SelectItem>
-   </SelectContent>
- </Select>

+ <OptionListTrigger label="Currency" onPress={() => sheetRef.current?.present()}>
+   {selected && <Text>{selected.label}</Text>}
+ </OptionListTrigger>
+ <BottomSheet ref={sheetRef}>
+   <OptionList items={items} value={value} onValueChange={(v) => { setValue(v); sheetRef.current?.dismiss(); }}>
+     <OptionListContent renderItem={...} />
+   </OptionList>
+ </BottomSheet>
```

#### 3. Component mapping cheat sheet

| Select | OptionList |
|---|---|
| `Select` | `OptionList` |
| `SelectTrigger` | `OptionListTrigger` |
| `SelectValue` | Render selected item yourself |
| `SelectContent` | `OptionListContent` (with `renderItem`) |
| `SelectItem` | `OptionListItem` |
| `SelectItemText` | `OptionListItemText` |
| `SelectGroup` + `SelectLabel` | `group` field on item data |
| `SelectSeparator` | Automatic between groups |
| `GlobalSelectProvider` | Your own `BottomSheet` |
| — | `OptionListItemDescription` (new) |
| — | `OptionListItemLeading` (new) |
| — | `OptionListItemContent` (new) |
| — | `OptionListItemContentRow` (new) |
| — | `OptionListEmptyState` (new) |
