name: Figma Code Connect

on:
  push:
    paths:
      - 'libs/ui-react/src/lib/Components/**/*.figma.tsx'
      - 'libs/ui-react/figma.config.json'
      - 'libs/ui-rnative/src/lib/Components/**/*.figma.tsx'
      - 'libs/ui-rnative/figma.config.json'

permissions:
  contents: read

concurrency:
  group: figma-code-connect-${{ github.ref }}
  cancel-in-progress: true

jobs:
  code-connect:
    name: Code Connect (${{ matrix.library }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - library: ui-react
            config: libs/ui-react/figma.config.json
          - library: ui-rnative
            config: libs/ui-rnative/figma.config.json
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup
        with:
          cache-nx: 'false'
          set-shas: 'false'

      - name: Publish to Figma Code Connect
        run: npx figma connect publish --config ${{ matrix.config }} --exit-on-unreadable-files
        env:
          FIGMA_ACCESS_TOKEN: ${{ secrets.FIGMA_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
