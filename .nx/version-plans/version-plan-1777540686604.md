---
'@ledgerhq/lumen-ui-rnative': patch
---

feat(MediaImage): add `fallback` prop to display a single-letter initial when no `src` is provided or the image fails to load
feat(MediaImage): add `loading` prop to show a `Skeleton` pulsing overlay while content is loading
feat(MediaImage): add 1px inset outline on the container for improved visual consistency across backgrounds
fix(DotIcon): update size mapping for DotIcon from 12 to 16 for size 20