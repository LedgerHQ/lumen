---
'@ledgerhq/lumen-ui-react': patch
---

feat(Navbar): Add `NavBarDescription` and `NavBarLeading` sub-components to `NavBar`, enabling descriptive text next to the title.
fix(Subheader): Increase ChevronRight icon size from 20 to 24 for better visibility.

