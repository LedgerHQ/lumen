import { Meta } from '@storybook/addon-docs/blocks';
import { SizeTable } from './SizeTable';

<Meta title='Style System/Theme/Sizes' />

# 📐 Sizes

Size tokens control the dimensions of elements through width and height properties.

## Available Sizes

Size tokens are accessed through the `width`, `height`, `minWidth`, `minHeight`, `maxWidth`, and `maxHeight` props in the `lx` style object.

<SizeTable />

