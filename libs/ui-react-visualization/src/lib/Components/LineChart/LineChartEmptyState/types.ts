export type LineChartEmptyStateProps = {
  /**
   * Whether to animate the placeholder line with a shimmer (initial loading).
   * When false, the placeholder is static (empty / no-data state).
   * @default false
   */
  loading?: boolean;
};
