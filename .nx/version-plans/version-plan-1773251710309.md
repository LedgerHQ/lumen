---
'@ledgerhq/lumen-ui-rnative': patch
'@ledgerhq/lumen-ui-react': patch
---

BREAKING_CHANGE: migrate Tile to compound API

TileSpot has been removed from `@ledgerhq/lumen-ui-react`. Use the shared Spot component instead.
Replace `<TileSpot />` with `<Spot />`.
