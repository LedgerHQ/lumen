---
'@ledgerhq/lumen-ui-react': patch
---

BREAKING_CHANGE(ui-react): use "expanded" instead of "extended" for Dialog component

If you use the Dialog component with the `extended` prop or variant, replace it with `expanded`.
