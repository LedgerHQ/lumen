---
'@ledgerhq/lumen-ui-react': patch
---

chore(storybook): adapt config to surface the visualization docs

Adapt the Storybook configuration to load the visualization package's `.mdx`
guides and generate Controls for its components, and add a small `DocTable`
documentation helper. No public component or API changes.
